[project]
name = "pythonplaywright"
version = "0.1.0"
description = "Python Playwright E2E test automation UI + API project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pip>=26.0.1",
    "playwright>=1.58.0",
    "pytest>=9.0.2",
    "pytest-playwright>=0.7.2",
]
